# CloudflareSpeedTest 项目结构

## 📋 项目概述

**CloudflareSpeedTest** 是一个用 Go 语言编写的 Cloudflare CDN 速度测试工具。它可以测试 Cloudflare CDN（或其他 CDN）的各个 IP 地址的延迟和下载速度，帮助用户找到最快的 IP。

- **项目语言**: Go 1.18+
- **主要功能**: TCP 延迟测速、HTTP 延迟测速、下载速度测试
- **输出格式**: CSV 文件

---

## 📁 项目结构详解

```
CloudflareSpeedTest/
├── main.go                 # 项目入口文件
├── go.mod                  # Go 模块定义
├── go.sum                  # Go 依赖版本锁定
├── README.md               # 项目说明文档
├── LICENSE                 # 项目许可证
├── ip.txt                  # IPv4 地址段文件（输入数据）
├── ipv6.txt                # IPv6 地址段文件（输入数据）
│
├── task/                   # 核心任务模块（延迟测速、下载测速）
│   ├── ip.go              # IP 地址解析模块
│   ├── tcping.go          # TCP 延迟测速实现
│   ├── httping.go         # HTTP 延迟测速实现
│   └── download.go        # 下载速度测试实现
│
├── utils/                  # 工具函数模块
│   ├── csv.go             # CSV 结果输出处理
│   ├── color.go           # 控制台彩色输出
│   └── progress.go        # 进度条显示
│
├── script/                 # 辅助脚本（各平台特定用途）
│   ├── README.md          # 脚本说明文档
│   ├── cfst_hosts.sh      # Linux/Mac 添加 hosts 脚本
│   ├── cfst_hosts.bat     # Windows 添加 hosts 脚本
│   ├── cfst_hosts_mac.sh  # Mac 特定 hosts 脚本
│   ├── cfst_3proxy.bat    # Windows 3proxy 代理配置
│   ├── cfst_dnspod.sh     # DNSPod API 更新脚本（Linux/Mac）
│   ├── cfst_dnsmasq.sh    # Dnsmasq 配置脚本
│   ├── cfst_ddns.sh       # 动态 DNS 更新脚本（Linux/Mac）
│   └── cfst_ddns.bat      # 动态 DNS 更新脚本（Windows）
│
└── .github/                # GitHub 配置
    └── ISSUE_TEMPLATE/     # Issue 模板（Bug 报告、功能请求等）
```

---

## 📄 核心文件详解

### **main.go** - 项目入口
- **功能**: 命令行参数解析、程序流程控制
- **职责**:
  - 解析用户输入的命令行参数（延迟测速线程数、次数、下载测速配置等）
  - 调用 task 模块进行测试
  - 输出测试结果到 CSV 文件
- **支持的主要参数**:
  - `-n`: 延迟测速线程数（默认 200）
  - `-t`: 单个 IP 延迟测速次数（默认 4）
  - `-dn`: 下载测速 IP 数量（默认 10）
  - `-dt`: 单个 IP 下载测速时间（默认 10 秒）
  - `-tp`: 测速端口（默认 443）
  - `-url`: 测速地址
  - `-httping`: 使用 HTTP 协议模式（默认 TCP）
  - `-cfcolo`: 指定 Cloudflare 地区码过滤

### **go.mod & go.sum** - 依赖管理
- **功能**: Go 模块依赖声明和锁定
- **主要依赖**:
  - `github.com/VividCortex/ewma`: 指数加权移动平均（用于带宽计算）
  - `github.com/cheggaaa/pb/v3`: 进度条库
  - `github.com/fatih/color`: 彩色输出库

### **ip.txt & ipv6.txt** - 测试数据
- **功能**: 存储 Cloudflare 的 IP 地址段
- **格式**: 每行一个 IP 或 IP 段（CIDR 格式）
- **用途**: 程序从这些文件读取需要测试的 IP

---

## 📦 task 模块 - 核心测速功能

### **ip.go** - IP 地址处理
- **功能**: IP 地址段解析和随机 IP 生成
- **主要函数**:
  - `InitRandSeed()`: 初始化随机数种子
  - `isIPv4()`: 判断是否为 IPv4 地址
  - 从 ip.txt 和 ipv6.txt 读取 IP 段
  - 从 IP 段中随机生成测试 IP

### **tcping.go** - TCP 延迟测速
- **功能**: 通过 TCP 连接测试延迟
- **机制**: 
  - 创建与目标 IP 指定端口的 TCP 连接
  - 测量连接建立时间（RTT）
  - 多次测试求平均延迟
- **配置变量**:
  - `Routines`: 并发测速线程数（默认 200，最多 1000）
  - `TCPPort`: 测速端口（默认 443）
  - `PingTimes`: 单个 IP 测速次数（默认 4）

### **httping.go** - HTTP 延迟测速
- **功能**: 通过 HTTP 请求测试延迟
- **机制**:
  - 发送 HTTP HEAD/GET 请求到指定 URL
  - 测量 HTTP 响应时间
  - 支持指定有效的 HTTP 状态码
- **特色**: 支持按 Cloudflare 地区码（IATA 机场码）过滤

### **download.go** - 下载速度测试
- **功能**: 测试从指定 IP 的实际下载速度
- **机制**:
  - 连接到 IP 的 HTTP 服务器
  - 下载指定 URL 的内容
  - 计算下载速率（KB/s 或 MB/s）
  - 支持设置最长测试时间限制

---

## 🛠️ utils 模块 - 工具函数

### **csv.go** - 结果输出
- **功能**: 将测试结果导出为 CSV 文件
- **职责**:
  - 格式化测试数据（IP、延迟、丢包率、下载速度等）
  - 写入 CSV 文件（默认 result.csv）
  - 支持结果过滤（按延迟上下限、丢包率等）
- **配置变量**:
  - `InputMaxDelay`: 延迟上限（默认 9999 ms）
  - `InputMinDelay`: 延迟下限（默认 0 ms）
  - `InputMaxLossRate`: 丢包率上限（默认 1.0 即 100%）
  - `Output`: 输出文件名
  - `PrintNum`: 输出结果数量

### **color.go** - 彩色输出
- **功能**: 提供控制台彩色输出
- **用途**: 美化测试过程中的输出信息（红、绿、黄等颜色）
- **基于**: fatih/color 库

### **progress.go** - 进度条
- **功能**: 显示测试进度
- **用途**: 在测试过程中显示进度条和实时统计信息
- **基于**: cheggaaa/pb 库

---

## 📜 script 模块 - 辅助脚本

### **hosts 相关脚本**
- `cfst_hosts.sh`: Linux/Unix 系统添加优选 IP 到 hosts 文件
- `cfst_hosts.bat`: Windows 系统添加优选 IP 到 hosts 文件
- `cfst_hosts_mac.sh`: macOS 特定版本（处理 macOS 特定权限问题）
- **用途**: 将测试得出的最快 IP 写入系统 hosts 文件，实现域名解析优化

### **代理配置脚本**
- `cfst_3proxy.bat`: 为 3proxy 代理软件生成配置文件
- **用途**: 自动配置代理软件使用优选 IP

### **DNS 更新脚本**
- `cfst_dnspod.sh`: 通过 DNSPod API 自动更新 DNS 记录
- `cfst_dnsmasq.sh`: 为 Dnsmasq DNS 服务器生成配置
- `cfst_ddns.sh/cfst_ddns.bat`: 动态 DNS 更新脚本
- **用途**: 自动将优选 IP 更新到 DNS 配置，实现自动化优化

---

## 🔄 工作流程

```
1. 读取 ip.txt 和 ipv6.txt
   ↓
2. 解析 IP 地址段，生成测试 IP 列表
   ↓
3. TCP/HTTP 延迟测速（可并发，默认 200 线程）
   ↓
4. 根据延迟结果排序，选取最优 IP
   ↓
5. 对最优 IP 进行下载速度测试
   ↓
6. 结果汇总、过滤
   ↓
7. 导出 CSV 结果文件
   ↓
8. （可选）运行 script/ 中的脚本自动应用优选 IP
```

---

## 📊 测试结果（result.csv）示例

| IP | 延迟(ms) | 下载速度(MB/s) | 丢包率(%) | 地区 |
|----|---------|--------------|---------|------|
| 1.2.3.4 | 25 | 85.2 | 0 | 香港 |
| 1.2.3.5 | 28 | 82.1 | 0 | 新加坡 |

---

## 🎯 项目特点

- ✅ **高效并发**: 支持最多 1000 个并发线程测速
- ✅ **多种测速模式**: TCP 和 HTTP 两种延迟测速方式
- ✅ **灵活过滤**: 支持按延迟、丢包率等条件过滤结果
- ✅ **自动化支持**: 提供脚本快速应用优选 IP 到系统
- ✅ **跨平台**: 支持 Windows、Linux、macOS
- ✅ **轻量级**: 单一二进制文件，无其他依赖

---

## 🚀 基本使用

```bash
# 编译项目
go build

# 基本测速（默认配置）
./CloudflareSpeedTest

# 自定义配置
./CloudflareSpeedTest -n 200 -t 4 -dn 10 -dt 10 -tp 443

# 使用 HTTP 模式
./CloudflareSpeedTest -httping -url "https://cf.xiu2.xyz/url"

# 指定 Cloudflare 地区
./CloudflareSpeedTest -cfcolo HKG,SGP,NRT
```

---

**更新时间**: 2026-04-10  
**项目地址**: https://github.com/XIU2/CloudflareSpeedTest